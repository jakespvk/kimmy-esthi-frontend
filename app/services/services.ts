import { fetchServices } from "../api";
import { Service, ServiceType } from "../types";

// Signature Facials
// export const signatureFacials: Service[] = await fetchServices(ServiceCardType.Facial);
// export const facialPackages: Service[] = await fetchServices(ServiceCardType.Package);
// export const serviceAddOns: Service[] = await fetchServices(ServiceCardType.AddOn);
